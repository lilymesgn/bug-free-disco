# ============================================================
# Fit Tracker PRO — Environment Variables (local/deployment)
# This file contains your actual project values.
# Do not commit this file if your repo is public — use
# .env.example as the template for other environments.
# ============================================================

# ── Supabase ───────────────────────────────────────────────────────────────
# Project URL and anon (publishable) key. The anon key is safe to expose in
# client-side code — it is restricted by Row Level Security policies defined
# in supabase/migrations/0001_init.sql. It can only do what RLS allows.
VITE_SUPABASE_URL=https://vpjtpbhrlqcwuoigukft.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_RB_y-_A6d5L7bXEZxbNfTg_rJ7J50vZ

# ── Stripe (optional — see .env.example for setup instructions) ─────────────
# VITE_STRIPE_PAYMENT_LINK_MONTHLY=
# VITE_STRIPE_PAYMENT_LINK_YEARLY=
